services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pdsl-14-python-app
    ports:
      - "8314:8080"
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/health', timeout=3).read()\""]
      interval: 10s
      timeout: 3s
      retries: 5
